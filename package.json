{
  "name": "speedcubing-central",
  "version": "1.0.0",
  "private": true,
  "description": "Speedcubing Central — a full-stack speedcubing platform",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "start": "prisma db push --accept-data-loss && node server/dist/index.js",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace client",
    "build:shared": "npm run build --workspace shared",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace server && npm run typecheck --workspace client",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:push": "prisma db push",
    "db:up": "docker compose up -d",
    "db:embedded:init": "node scripts/start-embedded-pg.mjs init",
    "db:embedded": "node scripts/start-embedded-pg.mjs",
    "db:setup": "prisma db push && npm run seed --workspace server",
    "prisma:seed": "npm run seed --workspace server",
    "prisma:studio": "prisma studio",
    "postinstall": "npm run build --workspace shared && prisma generate"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "embedded-postgres": "^18.4.0-beta.17",
    "prisma": "^5.22.0"
  },
  "engines": {
    "node": ">=20"
  }
}
